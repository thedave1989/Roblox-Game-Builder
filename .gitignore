# Per-machine Claude Code overrides — never commit
.claude/settings.local.json
CLAUDE.local.md

# Session counters (session-nudge hook scratch)
.claude/.tmp/

# OS noise
Thumbs.db
.DS_Store
*.log

# Studio-gate approval ledger (per-machine build state)
game/.builder/approved.json

# Python bytecode
__pycache__/
*.pyc
